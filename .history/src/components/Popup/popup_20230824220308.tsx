import React from 'react';
import styles from "./popup.module.scss"

export const Popup = () => {
    return (
        <div className={styles.modal}>
            Это какой то попап
        </div>
    );
};

